# CASTAWAY

무인도 생존 게임. **단독 HTML · Three.js r128 · 빌드 과정 없음.**

## 스택

- Three.js r128을 cdnjs에서 `<script src>`로 직접 로드. 게임에는 번들러·의존성·`node_modules`가 없다
  (`node_modules/`는 CI 검사가 playwright·three를 받을 때만 생긴다. `.gitignore`에 들어 있다)
- 본 게임은 `island_world.html` 한 파일(6,400줄) 안에 전부 들어 있다. 모듈 분할되어 있지 않다
- 아레나·건축장도 각각 한 파일로 독립되어 있다. 서로 코드를 공유하지 않고 복사해 쓴다
- 라이선스 GPL-3.0

## 실행

빌드 없음. 파일을 열면 끝이다.

```
island_world.html
```

지금 있는 파일은 전부 `file://`에서 그냥 열린다 — 아레나는 GLB를 base64로 파일 안에 박아
두었고 본 게임은 GLB를 아예 읽지 않는다. `.glb`를 **외부 파일로** 로드하는 코드를 넣는
순간부터는 CORS로 막히므로 정적 서버가 필요하다:

```bash
python -m http.server 5173
```

## 파일

| 파일 | 크기 | 내용 |
|---|---|---|
| `island_world.html` | 0.3MB | 메인 게임. 아이템 100종 · 레시피 91개 |
| `build_sandbox.html` | 0.3MB | 건축장. 평지 · 재료 무제한 · 무게 제한 없음 |
| `bear_arena.html` | 1.6MB | 북극곰 전투 시험장. 내려찍기 |
| `gargoyle_arena.html` | 4.9MB | 곤충형 가고일 전투 시험장 |
| `mixed_arena.html` | 6.4MB | 섞인 시험장. 곰 1 · 가고일 3, 입장 전 장비 선택 |
| `CASTAWAY_개발요약.md` | | **설계 문서. 작업 전에 먼저 읽을 것** — 지형·제작 트리·설비·몹·저장 |
| `CASTAWAY_전사_초반부.md`, `CASTAWAY_전사_2차.md` | | 개발 대화 원문 |
| `tools/check-syntax.mjs`, `tools/smoke.mjs` | | 검사 도구 (아래 참고) |

아레나 셋 모두 `WEAPONS` 표에 무기 10종을 똑같이 들고 있다 (숫자키 1~9·0, 소총은 X로 재장전).

몹 모델 `.glb`는 **본 게임이 하나도 읽지 않는다.** 아레나는 원본 파일이 아니라
파일 안에 박힌 base64 사본을 쓴다 — 즉 `.glb` 원본과 아레나 안의 사본이 따로 논다.

| 모델 | 크기 | 쓰는 곳 |
|---|---|---|
| `bear_high.glb` | 1.1MB | `bear_arena.html` · `mixed_arena.html`에 base64로 각각 내장 |
| `stormscale_wyrm.glb` | 8.5MB | **없음** |
| `stone_colossus.glb` | 16.5MB | **없음** (문서에만 이름이 나온다) |

### 안에서 길 찾기

`island_world.html`의 큰 뼈대는 `====` 로 둘러싼 **번호 붙은 절**이다. 이게 1차 지도다.

```
1) 기반 지형   2) 열적 침식   3) 수적 침식   4) 수문   5) 경사·거리장   6) 기후
7) 바이옴 분류   8) 자원 배치   9) 3D 월드 렌더링   10) 아이템·인벤토리
11) 맨손 채집   12) 제작 (12-b 불 · 12-c 근접 공격 · 12-d 사전 · 12-d 몹 스폰)
13) 1인칭 플레이어   11) HUD   12) 초기화·생성·루프
```

**번호를 믿지 말고 제목으로 찾을 것.** 위에서 보듯 번호가 어긋나 있다 —
`12-d)`가 사전과 몹 스폰 둘에 붙어 있고, `12-c)`가 `12-d)` 뒤에 오고,
끝의 HUD·루프가 `11)` `12)`로 다시 시작한다.

절 안은 `/* ---------------- 제목 ---------------- */` (시드 PRNG & 노이즈 · 그리드 규격 ·
전역 필드 · 바이옴 정의 · 자원(지질 규칙) · 파라미터 · 하늘·광원·시간 · 발소리 · 입력 ·
설정 패널 · 메인 루프), 짓기 쪽은 더 짧은 `/* ---- 제목 ---- */`
(조각 만들기 · 놓기·걷어내기 · 어디에 놓을지 · 짓기 모드)로 다시 나뉜다.

표를 바로 찾을 거면 이름으로 —
`ITEMS`(100) `RECIPES`(91) `RESOURCES` `BIOME_INFO` `GEO`(사전) `HARVEST` `STATION`
`FIRES` `FURNACES` `STORES` `QUICK`(빠른 칸).

## 검사

푸시할 때마다 `.github/workflows/ci.yml`이 둘을 돌린다. 고치고 나서 직접 돌려 볼 것.

```bash
node tools/check-syntax.mjs *.html      # 인라인 스크립트 문법만. 몇 초
node tools/smoke.mjs [파일…]            # 헤드리스로 실제로 띄워 본다. 파일당 4~20초
```

- **문법 검사**는 브라우저 없이 `node --check`만 돌린다. 오타 하나로 게임이 안 뜨는 걸 막는다
- **구동 검사**는 크로뮴에 띄워 `pageerror`·콘솔 오류가 없는지 본다. 문법은 맞는데 실행하다
  죽는 경우를 잡는다. 파일마다 '다 떴다'고 볼 조건이 다르므로
  (본 게임은 `busy===false`, 아레나는 모델 로드) 파일을 추가하면 `tools/smoke.mjs`의
  `PAGES`에 조건을 함께 넣을 것. 안 넣으면 검사에서 빠진다
- 구동 검사는 CDN 요청을 가로채 `node_modules`의 같은 판(r128)으로 돌려준다.
  망이 막힌 곳에서도 돈다. `CHROMIUM_PATH`로 실행 파일을 지정할 수 있다

## 규칙

- **지형은 시드 하나에서 결정론적으로 생성된다.** 같은 시드는 언제나 같은 섬이어야 한다.
  지형·자원 배치 경로에서 `Math.random()`을 쓰지 말고 시드 PRNG를 쓸 것.
  저장 파일이 시드 + 변경분만 담아 8.8KB인 것도 이 성질에 기대고 있다 —
  깨뜨리면 기존 세이브가 전부 다른 섬을 연다.
  (겉모습만 바뀌는 곳 — 놓인 메시의 회전, 별, 소리 — 에는 `Math.random()`을 써도 된다.
  세이브에 안 들어가고 섬 모양을 바꾸지 않기 때문이다)
- **프레임률에 의존하는 로직을 만들지 말 것.** 이동 판정이 한 번 이 문제로 깨졌다
  (`높이차/프레임이동거리` → 지형 경사 기반으로 교체, 오판율 0.11% → 0%).
  거리·속도·시간 판정은 프레임당 이동량이 아니라 물리량으로 쓴다.
- **상자·화덕·제련로는 같은 용기다.** 슬롯 배열과 `contAdd`/`contTake`/`contFuel`을
  공유한다. 설비를 추가할 때 넣고 빼는 규칙을 따로 만들지 말 것.
- 제작 트리는 8단계 순서를 건너뛸 수 없게 되어 있다
  (풀과 돌 → 불 → 옷 → 나무와 점토 → 간석기 → 금속 → 철기 → 화학).
  레시피를 추가하면 재료→산출 의존성 기계 검증에 걸리는지 확인할 것.
  `RECIPES`에는 이 8개 밖에 `뼈와 키틴`이 하나 더 있다 — 순서 위의 단계가 아니라
  사냥으로 갈라지는 갈래다(레시피 7개). 순서 검증에 섞지 말 것.
- **`tier`라는 이름을 두 표가 다른 뜻으로 쓴다.** `RECIPES.tier`는 위의 진행 단계지만,
  사전 표 `GEO`의 `tier`는 `맨손` · `경도 1/2/3` — 그 원석을 캐는 데 필요한 도구 경도다.
- **불러오기는 없어진 아이템 id를 걸러야 한다.** 소지품 슬롯을 거르면서 **빠른 칸** 인덱스도
  같은 규칙으로 옮겨야 한다 — 안 옮기면 버린 아이템 수만큼 빠른 칸이 밀린다.
  `island_world.html`과 `build_sandbox.html` 양쪽에 같은 코드가 있다.

## 함정

- **같은 코드가 여러 파일에 복사돼 있다. 공유 모듈이 없으니 한쪽만 고치면 갈라진다.**
  - 소지품 · 제작 · 짓기 · 저장 → `island_world.html` + `build_sandbox.html`
  - 무기표와 근접 공격 → 아레나 셋 전부
  - 곰과 내려찍기 → `bear_arena.html` + `mixed_arena.html`
  - 가고일 → `gargoyle_arena.html` + `mixed_arena.html`

  아레나에는 소지품·제작·짓기가 아예 없다. 전투만 있는 파일이다.
- **`flatShading: true`는 `MeshLambertMaterial`에서 무시된다.** r128에서 확인해 14곳을 걷어냈다.
  각진 면이 필요하면 정점을 분리해야지 이 플래그로는 안 된다.
  (지금 남은 `flatShading` 두 군데는 주석이지 코드가 아니다)
- **Three.js를 cdnjs에서 받는다.** 오프라인에서는 실행되지 않는다.
- **레포가 40MB다** (GLB 26.1MB + 아레나 HTML 12.9MB, `.git`만 32MB). Git LFS를 쓰지 않는다.
  그중 실제로 쓰이는 건 곰 1.1MB뿐이고, 나머지 25MB는 아무 코드도 읽지 않는다.
  모델을 더 커밋하기 전에 **LFS 전환을 먼저 결정할 것** — 나중에 옮기려면 히스토리를 다시 써야 한다.
  모델을 base64로 HTML에 박으면 용량이 약 1.33배로 불어난다는 것도 같이 셈할 것.
- `.gitattributes`가 `* text=auto eol=lf`이므로 `*.glb binary`를 반드시 유지할 것.
